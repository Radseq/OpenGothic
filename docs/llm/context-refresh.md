# Compatibility: Context Retrieval

Use the root repository-index workflow, then the minimal reading path in
[`README.md`](README.md). Do not copy index commands, generated path lists or
search output into client context.
